#define MY_DEFINE "Copyright (c) 2023 Insoft. All rights reserved."

enum MyEnum {
    Red = 0,
    Blue = 1
}

fn brach() {
    if (A == B || B >= 10) {
        return -1;
    }
}

fn main() {
    var alpha; var beta;
    
    var text = MY_DEFINE;
    
    if (  (alpha ==  beta && alpha <= 0) || alpha == -1 ) {
        while (1) {
            alpha += 1;
        }
        for (a = 0; a < 10; a+=1) {
            
        }
        repeat {
            a -= 1;
        } until ( a == 0 );
    }
    else {
    }
    a += Red;
    
    var h = {6,8,4};
    return nil;
}
